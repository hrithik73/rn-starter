import { Text, View } from 'react-native';

const Login = () => {
  return (
    <View
      style={{
        flex: 1,
        margin: 10,
        flexDirection: 'row',
        gap: 10,
        alignItems: 'center',
      }}>
      <Text>Login</Text>
    </View>
  );
};
export default Login;
